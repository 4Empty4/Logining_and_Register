import { openModal, closeModal, switchModal } from './modal.js';

// Делаем функции глобально доступными для HTML
window.openModal = openModal;
window.closeModal = closeModal;
window.switchModal = switchModal;